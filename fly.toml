app = "kirogate"
primary_region = "nrt"

[build]
  # 使用外部已构建镜像时可以为空
  # image = "registry.fly.io/kirogate:latest"

[env]
  USER_DB_FILE = "/app/data/users.db"

[http_service]
  internal_port = 8000
  force_https = true

  auto_start_machines = true
  auto_stop_machines = "stop"

  # 🔴 调试阶段强烈建议 ≥ 1
  min_machines_running = 1

  processes = ["app"]

[[mounts]]
  source = "kirogate_data"
  destination = "/app/data"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
